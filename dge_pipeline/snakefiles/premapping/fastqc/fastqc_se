from os import listdir
from os.path import isfile, basename, splitext

rule all:
    input:
        expand("premapping/fastqc/{A}_fastqc.html", A=[splitext(basename(seqRun['main']['reads']))[0] for seqRun in config['entries'].values()])

rule fastqc:
    input:
        "preprocessing/{A}.fastq"
    output:
        "premapping/fastqc/{A}_fastqc.html",
        "premapping/fastqc/{A}_fastqc.zip"
    group:
        "fastqc"
    threads:
        1
    shell:
        "fastqc -o premapping/fastqc {input}"
