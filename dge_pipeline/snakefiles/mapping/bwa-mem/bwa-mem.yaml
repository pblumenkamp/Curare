name: 'bwa-mem'

required_settings:

  genome_fasta:
    description: 'Path to genome fasta file'
    type: 'file'

optional_settings:
  additional_bwa_options:
    description: "Additional options to use in shell command"
    type: "string"


single_end:
  snakefile: 'bwa-mem_se'

paired_end:
  snakefile: 'bwa-mem_pe'



